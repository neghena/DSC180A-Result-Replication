{
    "config": ["config/clean_gps_and_imu_data.json", "config/evaluation.json"],  # paths of at least 2 config files
    "build": "run.py", # path of your build script
    "library": ["src"], # directory containing (your) library code
    "notebook": ["notebooks/report.ipynb"] # path to at least one notebook meant for reading by someone other than you
    "targets": ["report", "target2"] # at least two targets
}
